package sample.usecase.event.type;

/** Expresses the type of notification. */
public enum NotificationType {
    /** Registration of transfer/withdrawal request completed */
    FINISH_REQUEST_WITHDRAW;
}
